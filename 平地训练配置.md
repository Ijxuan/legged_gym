# `minich_flat` 平地训练配置静态快照

> 范围：本文仅统计 `minich_flat` 平地任务的当前代码配置及本次启动参数，不包含动态训练结果、TensorBoard 指标或进程状态。
>
> 约定：除非特别说明，配置继承关系为 `MiniChFlatCfg → MiniChRoughCfg → LeggedRobotCfg`，PPO 配置继承关系为 `MiniChFlatCfgPPO → MiniChRoughCfgPPO → LeggedRobotCfgPPO`。每项末尾均给出可直接检索的“文件 :: 类/函数.变量”。
>
> 本次更新重点：静态核对目标**水平速度向量范数** `s = ||[v_x, v_y]||₂` 在 `0.2 m/s` 两侧的命令处理与奖励分支。它不是逐轴的 `|v_x|` 或 `|v_y|` 判断；本次未重新读取运行中进程。

## 1. 配置入口与当前启动参数

| 项目 | 当前值 | 代码定位 |
| --- | --- | --- |
| 任务别名 | `minich_flat`，注册为 `MiniCheetah + MiniChFlatCfg + MiniChFlatCfgPPO`；`MiniCheetah` 在 `LeggedRobot` 上增加启动期腿长形态随机化 | `legged_gym/envs/__init__.py :: task_registry.register("minich_flat", MiniCheetah, MiniChFlatCfg(), MiniChFlatCfgPPO())`；`legged_gym/envs/minich/minich.py :: MiniCheetah` |
| 环境创建 | 训练入口按 `--task` 取得任务配置并创建环境 | `legged_gym/scripts/train.py :: train`；`legged_gym/utils/task_registry.py :: TaskRegistry.make_env` |
| PPO runner 创建 | 使用同一任务名取得训练配置并创建 on-policy runner | `legged_gym/scripts/train.py :: train`；`legged_gym/utils/task_registry.py :: TaskRegistry.make_alg_runner` |
| 本次任务参数 | `--task=minich_flat --num_envs=2048 --max_iterations=4000 --sim_device=cuda:0 --rl_device=cuda:0 --seed=1 --run_name=flat_48obs_2048x4000` | `legged_gym/utils/helpers.py :: update_cfg_from_args` |
| 环境数的优先级 | 基类默认 `4096`，本次由 `--num_envs=2048` 覆写为 `2048` | `legged_gym/envs/base/legged_robot_config.py :: LeggedRobotCfg.env.num_envs`；`legged_gym/utils/helpers.py :: update_cfg_from_args` |
| 训练迭代数的优先级 | runner 默认 `1500`，本次由 `--max_iterations=4000` 覆写为 `4000` | `legged_gym/envs/base/legged_robot_config.py :: LeggedRobotCfgPPO.runner.max_iterations`；`legged_gym/utils/helpers.py :: update_cfg_from_args` |

## 2. 环境

| 项目 | 当前值 | 代码定位 |
| --- | --- | --- |
| 地形类型 | 平面 `plane` | `legged_gym/envs/minich/minich_flat_config.py :: MiniChFlatCfg.terrain.mesh_type` |
| 高度采样 | 关闭，不向观测加入高度网格 | `legged_gym/envs/minich/minich_flat_config.py :: MiniChFlatCfg.terrain.measure_heights`；`legged_gym/envs/base/legged_robot.py :: LeggedRobot.compute_observations` |
| 地形课程 | 关闭 | `legged_gym/envs/minich/minich_flat_config.py :: MiniChFlatCfg.terrain.curriculum` |
| 平面材料 | 静摩擦 `1.0`、动摩擦 `1.0`、恢复系数 `0.0` | `legged_gym/envs/base/legged_robot_config.py :: LeggedRobotCfg.terrain.static_friction/dynamic_friction/restitution`；`legged_gym/envs/base/legged_robot.py :: LeggedRobot._create_ground_plane` |
| 机器人 URDF | 源 URDF 为 `resources/robots/mini_cheetah/urdf/mini_cheetah.urdf`；当前任务启动时可据此生成腿长变体资产 | `legged_gym/envs/minich/minich_config.py :: MiniChRoughCfg.asset.file`；`legged_gym/envs/minich/minich.py :: MiniCheetah._load_robot_assets` |
| 足端名称 | `foot` | `legged_gym/envs/minich/minich_config.py :: MiniChRoughCfg.asset.foot_name` |
| 终止接触体 | 仅 `trunk` | `legged_gym/envs/minich/minich_config.py :: MiniChRoughCfg.asset.terminate_after_contacts_on` |
| 碰撞建模 | 不合并固定关节；内部自碰撞关闭 | `legged_gym/envs/minich/minich_config.py :: MiniChRoughCfg.asset.collapse_fixed_joints/self_collisions` |
| 初始根部位置 | `[0, 0, 0.30]` m | `legged_gym/envs/minich/minich_config.py :: MiniChRoughCfg.init_state.pos` |
| 基座硬高度下限 | 相对平面高度 `< 0.15` m 时立即 reset | `legged_gym/envs/minich/minich_config.py :: MiniChRoughCfg.env.min_base_height`；`legged_gym/envs/base/legged_robot.py :: LeggedRobot.check_termination` |
| 默认关节姿态 | 左前/左后髋关节 `+0.10` rad，右前/右后髋关节 `-0.10` rad；四个大腿 `-0.80` rad；四个小腿 `1.62` rad | `legged_gym/envs/minich/minich_config.py :: MiniChRoughCfg.init_state.default_joint_angles` |
| 控制器 | 位置 PD 控制，`control_type="P"` | `legged_gym/envs/minich/minich_config.py :: MiniChRoughCfg.control.control_type`；`legged_gym/envs/base/legged_robot.py :: LeggedRobot._compute_torques` |
| PD 刚度 | 普通关节 `17`，小腿关节 `34` | `legged_gym/envs/minich/minich_config.py :: MiniChRoughCfg.control.stiffness` |
| PD 阻尼 | 普通关节 `0.4`，小腿关节 `0.8` | `legged_gym/envs/minich/minich_config.py :: MiniChRoughCfg.control.damping` |
| 仿真步长 | `sim.dt = 0.005` s | `legged_gym/envs/base/legged_robot_config.py :: LeggedRobotCfg.sim.dt` |
| 控制抽帧 | `decimation = 4`，因此策略/控制步长 `dt = 0.005 × 4 = 0.02` s，频率 `50 Hz` | `legged_gym/envs/minich/minich_config.py :: MiniChRoughCfg.control.decimation`；`legged_gym/envs/base/legged_robot.py :: LeggedRobot._parse_cfg` |
| 物理求解 | PhysX TGS，位置迭代 `4`、速度迭代 `0`、`substeps=1`、重力 `-9.81 m/s²` | `legged_gym/envs/base/legged_robot_config.py :: LeggedRobotCfg.sim.physx/sim.substeps/sim.gravity` |
| 环境间距 | `3.0` m | `legged_gym/envs/base/legged_robot_config.py :: LeggedRobotCfg.env.env_spacing` |
| 单回合时长 | `20` s；名义上 `ceil(20 / 0.02) = 1000` 个策略步 | `legged_gym/envs/base/legged_robot_config.py :: LeggedRobotCfg.env.episode_length_s`；`legged_gym/envs/base/legged_robot.py :: LeggedRobot._parse_cfg` |

## 3. 网络结构与 PPO

| 项目 | 当前值 | 代码定位 |
| --- | --- | --- |
| Actor 输入 | `48` 维观测；无 privileged observation | `legged_gym/envs/minich/minich_flat_config.py :: MiniChFlatCfg.env.num_observations`；`legged_gym/envs/base/legged_robot_config.py :: LeggedRobotCfg.env.num_privileged_obs` |
| Actor MLP | `48 → 512 → 256 → 128 → 12`，每个隐藏层使用 `ELU`，输出层无 `tanh` | `legged_gym/envs/base/legged_robot_config.py :: LeggedRobotCfgPPO.policy.actor_hidden_dims/activation`；`rsl_rl/rsl_rl/modules/actor_critic.py :: ActorCritic.__init__` |
| Critic MLP | `48 → 512 → 256 → 128 → 1`；没有 privileged observation 时与 Actor 使用同一观测 | `legged_gym/envs/base/legged_robot_config.py :: LeggedRobotCfgPPO.policy.critic_hidden_dims`；`rsl_rl/rsl_rl/modules/actor_critic.py :: ActorCritic.__init__`；`rsl_rl/rsl_rl/runners/on_policy_runner.py :: OnPolicyRunner.__init__` |
| 策略分布 | 12 维高斯分布，动作标准差为可学习参数，初始值 `1.0` | `legged_gym/envs/base/legged_robot_config.py :: LeggedRobotCfgPPO.policy.init_noise_std`；`rsl_rl/rsl_rl/modules/actor_critic.py :: ActorCritic.std` |
| 每环境 rollout 长度 | `24` 个策略步，即 `0.48` s | `legged_gym/envs/base/legged_robot_config.py :: LeggedRobotCfgPPO.runner.num_steps_per_env` |
| 每迭代采样量 | `2048 × 24 = 49,152` 条环境转移 | `legged_gym/envs/base/legged_robot_config.py :: LeggedRobotCfgPPO.runner.num_steps_per_env`；`legged_gym/utils/helpers.py :: update_cfg_from_args` |
| PPO epoch / mini-batch | `5` 个 epoch、`4` 个 mini-batch；本次每个 mini-batch 为 `49,152 / 4 = 12,288` 条样本 | `legged_gym/envs/base/legged_robot_config.py :: LeggedRobotCfgPPO.algorithm.num_learning_epochs/num_mini_batches`；`rsl_rl/rsl_rl/algorithms/ppo.py :: PPO.update` |
| 学习率策略 | 初始学习率 `1e-3`，自适应调度，目标 KL `0.01` | `legged_gym/envs/base/legged_robot_config.py :: LeggedRobotCfgPPO.algorithm.learning_rate/schedule/desired_kl` |
| PPO 核心参数 | `gamma=0.99`、`lam=0.95`、clip 参数 `0.2`、熵系数 `0.01`、值函数系数 `1.0`、最大梯度范数 `1.0` | `legged_gym/envs/base/legged_robot_config.py :: LeggedRobotCfgPPO.algorithm.gamma/lam/clip_param/entropy_coef/value_loss_coef/max_grad_norm` |
| JIT 导出 | 每 `1000` 次迭代导出一次；正常退出时额外导出稳定的 latest 命名策略 | `legged_gym/envs/minich/minich_flat_config.py :: MiniChFlatCfgPPO.runner.jit_export_interval/jit_export_on_exit` |

## 4. 输入与输出

### 4.1 策略输入：48 维观测

| 下标范围 | 内容 | 输入缩放 | 代码定位 |
| --- | --- | --- | --- |
| `0:3` | 基座线速度 `base_lin_vel` | `× 2.0` | `legged_gym/envs/base/legged_robot.py :: LeggedRobot.compute_observations`；`legged_gym/envs/base/legged_robot_config.py :: LeggedRobotCfg.normalization.obs_scales.lin_vel` |
| `3:6` | 基座角速度 `base_ang_vel` | `× 0.25` | `legged_gym/envs/base/legged_robot.py :: LeggedRobot.compute_observations`；`legged_gym/envs/base/legged_robot_config.py :: LeggedRobotCfg.normalization.obs_scales.ang_vel` |
| `6:9` | 投影重力方向 `projected_gravity` | 不额外缩放 | `legged_gym/envs/base/legged_robot.py :: LeggedRobot.compute_observations` |
| `9:12` | 命令 `[v_x, v_y, yaw_rate]` | `[×2.0, ×2.0, ×0.25]` | `legged_gym/envs/base/legged_robot.py :: LeggedRobot.compute_observations`；`legged_gym/envs/base/legged_robot_config.py :: LeggedRobotCfg.normalization.obs_scales.lin_vel/ang_vel` |
| `12:24` | `dof_pos - default_dof_pos` | `× 1.0` | `legged_gym/envs/base/legged_robot.py :: LeggedRobot.compute_observations`；`legged_gym/envs/base/legged_robot_config.py :: LeggedRobotCfg.normalization.obs_scales.dof_pos` |
| `24:36` | 12 个关节速度 `dof_vel` | `× 0.05` | `legged_gym/envs/base/legged_robot.py :: LeggedRobot.compute_observations`；`legged_gym/envs/base/legged_robot_config.py :: LeggedRobotCfg.normalization.obs_scales.dof_vel` |
| `36:48` | 上一策略步动作 `actions` | 不额外缩放 | `legged_gym/envs/base/legged_robot.py :: LeggedRobot.compute_observations` |

### 4.2 观测噪声与裁剪

| 项目 | 当前值 | 代码定位 |
| --- | --- | --- |
| 噪声总开关 | 开启，`noise_level = 1.0` | `legged_gym/envs/base/legged_robot_config.py :: LeggedRobotCfg.noise.add_noise/noise_level` |
| 线速度噪声 | 已结合观测缩放，均匀噪声幅值 `±0.20` | `legged_gym/envs/base/legged_robot.py :: LeggedRobot._get_noise_scale_vec`；`legged_gym/envs/base/legged_robot_config.py :: LeggedRobotCfg.noise.noise_scales.lin_vel` |
| 角速度噪声 | 已结合观测缩放，均匀噪声幅值 `±0.05` | `legged_gym/envs/base/legged_robot.py :: LeggedRobot._get_noise_scale_vec`；`legged_gym/envs/base/legged_robot_config.py :: LeggedRobotCfg.noise.noise_scales.ang_vel` |
| 重力方向噪声 | 均匀噪声幅值 `±0.05` | `legged_gym/envs/base/legged_robot.py :: LeggedRobot._get_noise_scale_vec`；`legged_gym/envs/base/legged_robot_config.py :: LeggedRobotCfg.noise.noise_scales.gravity` |
| 关节位置噪声 | 均匀噪声幅值 `±0.01` | `legged_gym/envs/base/legged_robot.py :: LeggedRobot._get_noise_scale_vec`；`legged_gym/envs/base/legged_robot_config.py :: LeggedRobotCfg.noise.noise_scales.dof_pos` |
| 关节速度噪声 | 已结合观测缩放，均匀噪声幅值 `±0.075` | `legged_gym/envs/base/legged_robot.py :: LeggedRobot._get_noise_scale_vec`；`legged_gym/envs/base/legged_robot_config.py :: LeggedRobotCfg.noise.noise_scales.dof_vel` |
| 无噪声字段 | 命令和上一动作不加观测噪声 | `legged_gym/envs/base/legged_robot.py :: LeggedRobot._get_noise_scale_vec` |
| 观测裁剪 | 最终观测裁剪到 `[-100, 100]` | `legged_gym/envs/base/legged_robot_config.py :: LeggedRobotCfg.normalization.clip_observations`；`legged_gym/envs/base/legged_robot.py :: LeggedRobot.compute_observations` |

### 4.3 策略输出与关节控制

| 项目 | 当前值 | 代码定位 |
| --- | --- | --- |
| 动作维度 | `12` 维，每个仿真 DOF 一个动作；四条腿各有髋、大腿、小腿三个关节 | `legged_gym/envs/base/legged_robot_config.py :: LeggedRobotCfg.env.num_actions`；`resources/robots/mini_cheetah/urdf/mini_cheetah.urdf :: revolute joint declarations` |
| 动作关节组 | `FR/FL/RR/RL × {hip, thigh, calf}`；实际索引跟随 URDF/Isaac Gym 的 DOF 声明顺序 | `resources/robots/mini_cheetah/urdf/mini_cheetah.urdf :: FR_*/FL_*/RR_*/RL_*_joint`；`legged_gym/envs/base/legged_robot.py :: LeggedRobot._create_envs` |
| 动作裁剪 | 输入物理仿真前裁剪到 `[-100, 100]` | `legged_gym/envs/base/legged_robot_config.py :: LeggedRobotCfg.normalization.clip_actions`；`legged_gym/envs/base/legged_robot.py :: LeggedRobot.step` |
| 目标位置 | `q_target = default_dof_pos + 0.25 × action` | `legged_gym/envs/minich/minich_config.py :: MiniChRoughCfg.control.action_scale`；`legged_gym/envs/base/legged_robot.py :: LeggedRobot._compute_torques` |
| 力矩计算 | `tau = Kp × (q_target - q) - Kd × qdot`，随后裁剪到 URDF 力矩上限 | `legged_gym/envs/base/legged_robot.py :: LeggedRobot._compute_torques`；`resources/robots/mini_cheetah/urdf/mini_cheetah.urdf :: effort limits` |

### 4.4 目标 xy 速度 `0.2 m/s` 阈值核对（本次重点）

令 `s_raw = ||[v_x_raw, v_y_raw]||₂`，`s = ||[v_x, v_y]||₂`。代码对 xy **向量范数**使用严格条件 `s_raw > 0.2`，而非逐轴绝对值比较：

```python
commands[:, :2] *= (norm(commands[:, :2]) > 0.2)
```

代码定位：`legged_gym/envs/base/legged_robot.py :: LeggedRobot._resample_commands`。

| 采样/处理阶段 | 精确行为 | 代码定位 |
| --- | --- | --- |
| 原始命令采样 | `v_x_raw`、`v_y_raw` 独立均匀采样于 `[-1, 1] m/s`；`yaw_rate` 独立均匀采样于 `[-1, 1] rad/s` | `legged_gym/envs/base/legged_robot_config.py :: LeggedRobotCfg.commands.ranges`；`legged_gym/envs/base/legged_robot.py :: LeggedRobot._resample_commands` |
| `s_raw ≤ 0.2` | 两个 xy 命令都被置为 `0`，所以处理后 `s=0`；`s_raw=0.2` 也归入此分支 | `legged_gym/envs/base/legged_robot.py :: LeggedRobot._resample_commands` |
| `s_raw > 0.2` | 保留采样到的 xy 命令，因此处理后 `s=s_raw>0.2` | `legged_gym/envs/base/legged_robot.py :: LeggedRobot._resample_commands` |
| 强制零命令 | 独立以 `zero_command_probability=0.30` 将 `[v_x,v_y,yaw_rate]` 全部置零；它会覆盖上述任一原始速度分支 | `legged_gym/envs/minich/minich_config.py :: MiniChRoughCfg.commands.zero_command_probability`；`legged_gym/envs/base/legged_robot.py :: LeggedRobot._resample_commands` |
| yaw 的特殊性 | `MiniChFlatCfg.commands.heading_command=False`，故 yaw 为直接采样值；xy 被置零时，yaw 仍可非零，除非命中 30% 强制零命令 | `legged_gym/envs/minich/minich_flat_config.py :: MiniChFlatCfg.commands.heading_command`；`legged_gym/envs/base/legged_robot.py :: LeggedRobot._resample_commands` |

| 常见误解 | 实际结果 |
| --- | --- |
| `|v_x|<0.2` 且 `|v_y|<0.2` 就会归零 | 不一定。例：`[0.19, 0.19]` 的范数约为 `0.269>0.2`，会保留。 |
| 其中一个分量 `<0.2` 就会归零 | 不会。例：`[0.10, 0.30]` 的范数约为 `0.316>0.2`，会保留。 |
| 存在有效的 `0<s≤0.2` 慢走命令 | 不存在；该区间在重采样后会被压成精确的 `s=0`。 |

处理后只有两类 xy 命令：`s=0` 或 `s>0.2`（另加 30% 强制的全三维零命令）。原始 xy 在单位正方形内均匀采样时，约 `π×0.2²/4≈3.14%` 会因该阈值被置零；再叠加 30% 强制零命令，理论上约 `32.20%` 的重采样结果为 `s=0`，其中 30% 为完整的三维零命令。代码定位：`legged_gym/envs/base/legged_robot.py :: LeggedRobot._resample_commands`。

| 处理后速度区间 | 速度跟踪 | 足端滞空项 | 低速姿态/支撑项 | 零命令默认姿态 |
| --- | --- | --- | --- | --- |
| `s=0`（来自 `s_raw≤0.2` 或强制零） | 跟踪目标 xy 为 `0` | 禁用 | 姿态项立即生效；低速支撑/载荷项在 reset 后 `0.5 s` 生效 | 仅当 `||[v_x,v_y,yaw_rate]||<0.1`；所以 xy 为零但 yaw 非零时通常不生效 |
| `0.2<s≤0.25` | 跟踪非零 xy | 启用 | **仍生效**：姿态项立即生效，低速支撑项在 `0.5 s` 后生效；这是“步态项已开、低速支撑项未关”的重叠带 | 不生效 |
| `s>0.25` | 跟踪非零 xy | 启用 | 姿态与低速支撑项关闭 | 不生效 |

上表的 `0.25 m/s` 是另一个独立门槛：`orientation_command_threshold` 与 `low_speed_support_command_threshold` 都设为 `0.25`，因此“高于 `0.2`”不能笼统视为完全高速。代码定位：`legged_gym/envs/minich/minich_config.py :: MiniChRoughCfg.rewards.orientation_command_threshold/low_speed_support_command_threshold`；`legged_gym/envs/base/legged_robot.py :: LeggedRobot._reward_orientation/_low_speed_support_mask/_reward_feet_air_time`。

## 5. 奖励、惩罚与奖励缩放

奖励函数先将每个启用项乘以策略步长 `dt=0.02`，再求和；随后执行 `clip(sum, min=0)`。因此每一步的非终止奖励不会小于零。`termination` 项配置为零，故跌倒/终止没有额外的显式负奖励。

**统一有效系数：** `effective_scale = configured_scale × policy_dt = configured_scale × 0.02`。  
代码定位：`legged_gym/envs/base/legged_robot.py :: LeggedRobot._prepare_reward_function/compute_reward/_parse_cfg`。

### 5.1 正向奖励

| 奖励项 | 原始公式或触发条件 | 配置系数 | 每策略步有效系数 | 代码定位 |
| --- | --- | ---: | ---: | --- |
| 线速度跟踪 `tracking_lin_vel` | `exp(-||v_xy - cmd_xy||² / 0.25)` | `+1.0` | `+0.0200`，单步最大 `0.0200` | `legged_gym/envs/base/legged_robot_config.py :: LeggedRobotCfg.rewards.scales.tracking_lin_vel/tracking_sigma`；`legged_gym/envs/base/legged_robot.py :: LeggedRobot._reward_tracking_lin_vel` |
| 偏航角速度跟踪 `tracking_ang_vel` | `exp(-(ω_z - cmd_yaw)² / 0.25)` | `+0.5` | `+0.0100`，单步最大 `0.0100` | `legged_gym/envs/base/legged_robot_config.py :: LeggedRobotCfg.rewards.scales.tracking_ang_vel/tracking_sigma`；`legged_gym/envs/base/legged_robot.py :: LeggedRobot._reward_tracking_ang_vel` |
| 足端滞空 `feet_air_time` | 仅当 `s=||cmd_xy||>0.2` 时，在首次接触时累计 `(air_time - 0.5)`；低速 `s<0.2` 的接触判定要求 `Fz>10 N`，其余为 `Fz>1 N` | `+1.0` | `+0.0200` | `legged_gym/envs/minich/minich_config.py :: MiniChRoughCfg.rewards.feet_air_time_*`；`legged_gym/envs/base/legged_robot.py :: LeggedRobot._reward_feet_air_time` |

> `feet_air_time` 虽然权重为正，但在滞空时间小于 `0.5` s 时原始项可以为负；它不是固定的正向常数。代码定位：`legged_gym/envs/base/legged_robot.py :: LeggedRobot._reward_feet_air_time`。

### 5.2 惩罚项

| 惩罚项 | 原始公式或触发条件 | 配置系数 | 每策略步有效系数 | 代码定位 |
| --- | --- | ---: | ---: | --- |
| 垂直线速度 `lin_vel_z` | `v_z²` | `-2.0` | `-0.0400` | `legged_gym/envs/base/legged_robot_config.py :: LeggedRobotCfg.rewards.scales.lin_vel_z`；`legged_gym/envs/base/legged_robot.py :: LeggedRobot._reward_lin_vel_z` |
| 横滚/俯仰角速度 `ang_vel_xy` | `ω_x² + ω_y²` | `-0.05` | `-0.0010` | `legged_gym/envs/base/legged_robot_config.py :: LeggedRobotCfg.rewards.scales.ang_vel_xy`；`legged_gym/envs/base/legged_robot.py :: LeggedRobot._reward_ang_vel_xy` |
| 基座朝向 `orientation` | `projected_gravity_x² + projected_gravity_y²`；仅 `s≤0.25` 时生效，不看 yaw 命令 | `-1.0` | `-0.0200` | `legged_gym/envs/minich/minich_config.py :: MiniChRoughCfg.rewards.scales.orientation/orientation_command_threshold`；`legged_gym/envs/base/legged_robot.py :: LeggedRobot._reward_orientation` |
| 基座高度下限 `base_height` | reset 后前 `0.5 s` 不计；之后仅惩罚 `max(0.25 - base_height, 0)²`。`base_height_target=0.26` 仅为保留的兼容字段，当前实际使用下限 `0.25 m` | `-10.0` | `-0.2000` | `legged_gym/envs/minich/minich_config.py :: MiniChRoughCfg.rewards.scales.base_height/base_height_target/base_height_min/base_height_warmup_s`；`legged_gym/envs/base/legged_robot.py :: LeggedRobot._reward_base_height` |
| 低速缺失支撑脚 `low_speed_missing_support_feet` | reset 后 `0.5 s` 起，仅 `s≤0.25`；每只 `Fz≤10 N` 的足端计为缺失支撑脚 | `-1.0` | `-0.0200 × 缺失脚数` | `legged_gym/envs/minich/minich_config.py :: MiniChRoughCfg.rewards.scales.low_speed_missing_support_feet/foot_contact_force_threshold/low_speed_support_*`；`legged_gym/envs/base/legged_robot.py :: LeggedRobot._low_speed_support_mask/_reward_low_speed_missing_support_feet` |
| 低速载荷均衡 `low_speed_load_balance` | reset 后 `0.5 s` 起，仅 `s≤0.25` 且四足均 `Fz>10 N`；惩罚 `Σ(Fz/ΣFz - 0.25)²` | `-5.0` | `-0.1000 × 不均衡量` | `legged_gym/envs/minich/minich_config.py :: MiniChRoughCfg.rewards.scales.low_speed_load_balance/foot_contact_force_threshold/low_speed_support_*`；`legged_gym/envs/base/legged_robot.py :: LeggedRobot._low_speed_support_mask/_reward_low_speed_load_balance` |
| 力矩 `torques` | `Σ tau²` | `-0.0002` | `-0.000004` | `legged_gym/envs/minich/minich_config.py :: MiniChRoughCfg.rewards.scales.torques`；`legged_gym/envs/base/legged_robot.py :: LeggedRobot._reward_torques` |
| 关节加速度 `dof_acc` | `Σ ((last_dof_vel - dof_vel) / 0.02)²` | `-2.5e-7` | `-5e-9` | `legged_gym/envs/base/legged_robot_config.py :: LeggedRobotCfg.rewards.scales.dof_acc`；`legged_gym/envs/base/legged_robot.py :: LeggedRobot._reward_dof_acc` |
| 动作变化 `action_rate` | `Σ (last_actions - actions)²` | `-0.01` | `-0.0002` | `legged_gym/envs/base/legged_robot_config.py :: LeggedRobotCfg.rewards.scales.action_rate`；`legged_gym/envs/base/legged_robot.py :: LeggedRobot._reward_action_rate` |
| 大腿碰撞 `thigh_collision` | 大腿接触力范数大于 `0.1 N` 的刚体计数 | `-1.0` | `-0.0200` | `legged_gym/envs/minich/minich_config.py :: MiniChRoughCfg.rewards.scales.thigh_collision/asset.thigh_name`；`legged_gym/envs/base/legged_robot.py :: LeggedRobot._reward_thigh_collision` |
| 小腿碰撞 `calf_collision` | 小腿接触力范数大于 `0.1 N` 的刚体计数 | `-1/3` | `-0.0066667` | `legged_gym/envs/minich/minich_config.py :: MiniChRoughCfg.rewards.scales.calf_collision/asset.calf_name`；`legged_gym/envs/base/legged_robot.py :: LeggedRobot._reward_calf_collision` |
| 关节限位 `dof_pos_limits` | 超过软限位后的越界量之和；软限位比例 `0.9` | `-10.0` | `-0.2000` | `legged_gym/envs/minich/minich_config.py :: MiniChRoughCfg.rewards.soft_dof_pos_limit/rewards.scales.dof_pos_limits`；`legged_gym/envs/base/legged_robot.py :: LeggedRobot._reward_dof_pos_limits` |
| 髋关节镜像对称 `hip_symmetry` | 两组左右髋关节镜像偏差的平方和 | `-5.0` | `-0.1000` | `legged_gym/envs/minich/minich_config.py :: MiniChRoughCfg.rewards.scales.hip_symmetry/hip_symmetry_pairs`；`legged_gym/envs/base/legged_robot.py :: LeggedRobot._reward_hip_symmetry` |
| 零命令默认姿态 `zero_command_default_pose` | 当 `||cmd_3|| < 0.1` 时，关节相对默认姿态的均方误差 | `-2.0` | `-0.0400` | `legged_gym/envs/minich/minich_config.py :: MiniChRoughCfg.rewards.scales.zero_command_default_pose/zero_command_threshold`；`legged_gym/envs/base/legged_robot.py :: LeggedRobot._reward_zero_command_default_pose` |

### 5.3 已禁用的奖励项

| 奖励项 | 配置系数 | 含义 | 代码定位 |
| --- | ---: | --- | --- |
| `termination` | `0` | 无显式终止惩罚 | `legged_gym/envs/base/legged_robot_config.py :: LeggedRobotCfg.rewards.scales.termination`；`legged_gym/envs/base/legged_robot.py :: LeggedRobot.compute_reward` |
| `collision` | `0` | 通用碰撞项关闭；Mini Cheetah 专用大腿/小腿碰撞项仍启用 | `legged_gym/envs/minich/minich_config.py :: MiniChRoughCfg.rewards.scales.collision`；`legged_gym/envs/base/legged_robot.py :: LeggedRobot._reward_collision` |
| `dof_vel` | `0` | 关节速度惩罚关闭 | `legged_gym/envs/base/legged_robot_config.py :: LeggedRobotCfg.rewards.scales.dof_vel` |
| `feet_stumble` | `0` | 足端绊倒惩罚关闭 | `legged_gym/envs/base/legged_robot_config.py :: LeggedRobotCfg.rewards.scales.feet_stumble` |
| `stand_still` | `0` | 静止惩罚关闭 | `legged_gym/envs/base/legged_robot_config.py :: LeggedRobotCfg.rewards.scales.stand_still` |

## 6. 预期目标总奖励

| 项目 | 结论 | 代码定位 |
| --- | --- | --- |
| 硬编码目标总奖励 | **没有** `target_reward`、早停奖励阈值或奖励门槛；训练由 `max_iterations=4000` 控制 | `legged_gym/scripts/train.py :: train`；`legged_gym/envs/base/legged_robot_config.py :: LeggedRobotCfgPPO.runner.max_iterations`；`rsl_rl/rsl_rl/runners/on_policy_runner.py :: OnPolicyRunner.learn` |
| 纯跟踪理论参考 | 完美线速度和偏航跟踪、忽略足端项且无惩罚时：`(0.020 + 0.010) × 1000 ≈ 30` 每回合 | `legged_gym/envs/base/legged_robot.py :: LeggedRobot._prepare_reward_function/_reward_tracking_lin_vel/_reward_tracking_ang_vel/_parse_cfg` |
| 该参考的边界 | `30` 不是最大可达总奖励、不是合格阈值，也不是停止条件；`s=0` 时足端滞空项固定为 `0`，`0.2<s≤0.25` 时仍有低速姿态/支撑惩罚，逐步非负截断会掩盖失败的负值 | `legged_gym/envs/base/legged_robot.py :: LeggedRobot._reward_feet_air_time/_low_speed_support_mask/compute_reward` |
| 实际总回报指标 | `Train/mean_reward` 是 runner 记录的回合累计回报均值，适合作为“总奖励”观察项 | `rsl_rl/rsl_rl/runners/on_policy_runner.py :: OnPolicyRunner.learn` |
| 分项日志含义 | `Episode/rew_*` 是回合分项累计值除以 `episode_length_s=20` 后的每秒归一化指标，不是总回报 | `legged_gym/envs/base/legged_robot.py :: LeggedRobot.reset_idx`；`legged_gym/envs/base/legged_robot_config.py :: LeggedRobotCfg.env.episode_length_s` |
| 建议的达成判据 | 回合长度接近名义 `20 s`，`Episode/rew_tracking_lin_vel` 接近 `1.0`、`Episode/rew_tracking_ang_vel` 接近 `0.5`，各惩罚分项接近 `0`，再结合 `Train/mean_reward` 判断 | `legged_gym/envs/base/legged_robot.py :: LeggedRobot.reset_idx/_reward_tracking_lin_vel/_reward_tracking_ang_vel` |

## 7. 训练设置

### 7.1 终止条件

| 条件 | 当前值 | 行为 | 代码定位 |
| --- | --- | --- | --- |
| 躯干接触终止 | `trunk` 接触力范数 `> 1 N` | 立即标记该环境 reset | `legged_gym/envs/minich/minich_config.py :: MiniChRoughCfg.asset.terminate_after_contacts_on`；`legged_gym/envs/base/legged_robot.py :: LeggedRobot.check_termination` |
| 倾倒终止 | 基座倾角 `> π/2` | 立即标记该环境 reset | `legged_gym/envs/minich/minich_flat_config.py :: MiniChFlatCfg.env.max_base_tilt`；`legged_gym/envs/base/legged_robot.py :: LeggedRobot.check_termination` |
| 基座过低终止 | 平地上根节点高度 `< 0.15` m | 立即标记该环境 reset；与 `0.25 m` 的软高度惩罚不同，这是硬下限 | `legged_gym/envs/minich/minich_config.py :: MiniChRoughCfg.env.min_base_height`；`legged_gym/envs/base/legged_robot.py :: LeggedRobot.check_termination` |
| 超时终止 | episode 计数超过最大 episode 步数 | 标记 `time_out_buf` 并 reset；名义长度为 `20 s / 1000` 策略步 | `legged_gym/envs/base/legged_robot_config.py :: LeggedRobotCfg.env.episode_length_s/send_timeouts`；`legged_gym/envs/base/legged_robot.py :: LeggedRobot._parse_cfg/check_termination` |
| 终止奖励 | `termination=0` | 终止本身不额外扣分；仍会因奖励下限截断得到 `0` | `legged_gym/envs/base/legged_robot_config.py :: LeggedRobotCfg.rewards.scales.termination/only_positive_rewards`；`legged_gym/envs/base/legged_robot.py :: LeggedRobot.compute_reward` |

### 7.2 课程设置

| 项目 | 当前值 | 行为 | 代码定位 |
| --- | --- | --- | --- |
| 地形课程 | `False` | reset 时不升级 terrain level；平地任务始终使用 `plane` | `legged_gym/envs/minich/minich_flat_config.py :: MiniChFlatCfg.terrain.curriculum/terrain.mesh_type`；`legged_gym/envs/base/legged_robot.py :: LeggedRobot.reset_idx/_update_terrain_curriculum` |
| 命令课程 | `False` | reset 时不扩展命令范围 | `legged_gym/envs/minich/minich_flat_config.py :: MiniChFlatCfg.commands.curriculum`；`legged_gym/envs/base/legged_robot.py :: LeggedRobot.reset_idx/update_command_curriculum` |
| 命令范围 | 原始采样为 `v_x,v_y ∈ [-1,1]` m/s，`yaw_rate ∈ [-1,1]` rad/s | xy 会经过 `s_raw>0.2` 的向量范数门槛；处理细节见 §4.4 | `legged_gym/envs/base/legged_robot_config.py :: LeggedRobotCfg.commands.ranges.lin_vel_x/lin_vel_y/ang_vel_yaw`；`legged_gym/envs/base/legged_robot.py :: LeggedRobot._resample_commands` |

### 7.3 域随机化与 reset

| 项目 | 当前值 | 行为 | 代码定位 |
| --- | --- | --- | --- |
| 机器人接触摩擦 | 开启，范围 `[0.5, 1.25]` | 环境创建时从 `64` 个摩擦桶中为每个环境分配刚体形状摩擦 | `legged_gym/envs/minich/minich_flat_config.py :: MiniChFlatCfg.domain_rand.randomize_friction`；`legged_gym/envs/base/legged_robot_config.py :: LeggedRobotCfg.domain_rand.friction_range`；`legged_gym/envs/base/legged_robot.py :: LeggedRobot._process_rigid_shape_props` |
| 基座质量 | 开启，额外质量范围 `[-1, 1]` kg | 环境创建时加到根刚体质量 | `legged_gym/envs/minich/minich_flat_config.py :: MiniChFlatCfg.domain_rand.randomize_base_mass/added_mass_range`；`legged_gym/envs/base/legged_robot.py :: LeggedRobot._process_rigid_body_props` |
| 腿长形态 | 开启；大腿 `[0.203, 0.215]` m、小腿 `[0.184, 0.196]` m，启动期生成 `32` 个变体 | 每个变体的四条腿共用一组大腿/小腿长度；变体分配到环境后，在该环境生命周期内固定 | `legged_gym/envs/minich/minich_flat_config.py :: MINI_CHEETAH_*_LINK_LENGTH_RANGE_M/MINI_CHEETAH_LEG_LENGTH_RANDOMIZATION_NUM_VARIANTS/MiniChFlatCfg.domain_rand.randomize_leg_lengths`；`legged_gym/envs/minich/minich.py :: MiniCheetah._load_robot_assets` |
| 外部推力 | 开启，每 `15` s 一次，最大 xy 速度幅值 `1.0 m/s` | 对所有环境根部 xy 速度随机设为 `[-1,1]` 区间 | `legged_gym/envs/minich/minich_flat_config.py :: MiniChFlatCfg.domain_rand.push_robots`；`legged_gym/envs/base/legged_robot_config.py :: LeggedRobotCfg.domain_rand.push_interval_s/max_push_vel_xy`；`legged_gym/envs/base/legged_robot.py :: LeggedRobot._post_physics_step_callback/_push_robots` |
| 命令重采样 | 每 `5` s | xy 向量范数 `≤0.2` 时置零 xy；另有 `30%` 概率置零全部三维命令；详见 §4.4 | `legged_gym/envs/minich/minich_config.py :: MiniChRoughCfg.commands.resampling_time/zero_command_probability`；`legged_gym/envs/base/legged_robot.py :: LeggedRobot._post_physics_step_callback/_resample_commands` |
| 关节 reset | `default_dof_pos × Uniform(0.5, 1.5)`，关节速度置零 | 每个 reset 重新采样关节初始位置 | `legged_gym/envs/base/legged_robot.py :: LeggedRobot._reset_dofs` |
| 根部 reset | 根部位置回到环境 origin 加初始位置；六维根部速度采样 `Uniform(-0.5,0.5)` | 每个 reset 重新设置根状态 | `legged_gym/envs/base/legged_robot.py :: LeggedRobot._reset_root_states` |

## 8. 阅读与使用注意事项

- 本文是当前工作区代码的**静态配置说明**，不是训练进度报告；未读取任何运行中进程、日志或指标。代码定位总入口为 `legged_gym/scripts/train.py :: train`。
- 平地任务虽然禁用了地形高度观测和课程，但仍保留摩擦、基座质量、腿长、外推速度、reset 姿态和命令采样等扰动。代码定位：`legged_gym/envs/minich/minich_flat_config.py :: MiniChFlatCfg.domain_rand/terrain/commands`；`legged_gym/envs/minich/minich.py :: MiniCheetah._load_robot_assets`。
- 核对低速表现时，必须按 §4.4 区分 `s=0`、`0.2<s≤0.25` 和 `s>0.25`：`0.2` 决定 xy 命令归零与足端滞空奖励，`0.25` 决定低速姿态/支撑塑形是否仍启用。
- 评估策略时不要只看总奖励：由于 `only_positive_rewards=True`，失败回合的负向奖励会被截断；应同时观察回合长度、速度跟踪分项、低速支撑/高度/姿态项和碰撞/限位惩罚。代码定位：`legged_gym/envs/base/legged_robot_config.py :: LeggedRobotCfg.rewards.only_positive_rewards`；`legged_gym/envs/base/legged_robot.py :: LeggedRobot.compute_reward/reset_idx`。
